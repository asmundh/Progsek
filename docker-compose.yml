version: "2"
services:
  

  db:
    build:
      context: ./mysql
      dockerfile: Dockerfile


  app:
    build: 
     context: ./src
     dockerfile: Dockerfile

    links:
      - db
    ports:
      - "8080:8080"
      - "443:443"
  
